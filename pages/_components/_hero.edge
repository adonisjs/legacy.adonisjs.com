<div class="relative py-20 hero bg-grey-100">
	<div class="mx-auto" style="max-width: 620px;">
		<h1 class="mb-4 text-5xl font-semibold">
			{{ title }}
		</h1>

		@if(description)
			<span class="text-2xl font-medium text-green-800">
				{{ description }}
			</span>
		@endif

		@!yield($slots.main())
	</div>
</div>
